import React from 'react';
import Navbar from '../components/Navbar';

const Profile = () => {
    return (
        <section>
            <Navbar />
            Profile
        </section>
    );
};

export default Profile;