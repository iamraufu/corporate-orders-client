import React from 'react';
import Navbar from '../components/Navbar';

const Orders = () => {
    return (
        <section>
            <Navbar />
            Orders
        </section>
    );
};

export default Orders;